// Gateway FTTH Keystore Configuration
signingConfigs {
    debug {
        storeFile file('GatewayFTTH.jks')
        storePassword 'log2space'
        keyAlias 'log2space'
        keyPassword 'log2space'
    }
    release {
        // NOTE: For development we reuse the debug keystore.
        // Update this block with your real release keystore when ready.
        storeFile file('GatewayFTTH.jks')
        storePassword 'log2space'
        keyAlias 'log2space'
        keyPassword 'log2space'
    }
}

